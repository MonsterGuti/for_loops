n = int(input())
even_numbers = 0
for _ in range(n):
    num = int(input())
    if num % 2 != 0:
        print(f"{num} is odd!")
        break
    else:
        even_numbers +=1
if even_numbers == n:
    print("All numbers are even.")
